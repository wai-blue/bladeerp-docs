{{ include('components/breadcrumbs.twig', {'last': 'Dashboard'}) }}

{# top buttons #}
<div class="row" style="margin-bottom: 20px;">
  <div class="col">
    {{ include('components/card-button.twig', {
      'text': 'Add Person',
      'icon': 'fas fa-user',
      'cssClass': 'card-blue',
      })
    }}
  </div>
  <div class="col">
    {{ include('components/card-button.twig', {
      'text': 'Add New Item',
      'icon': 'fas fa-box',
      'cssClass': 'card-blue',
      })
    }}
  </div>
  <div class="col">
    {{ include('components/card-button.twig', {
      'text': 'Add New Package',
      'icon': 'fas fa-pallet',
      'cssClass': 'card-blue',
      })
    }}
  </div>
  <div class="col">
    {{ include('components/card-button.twig', {
      'text': 'Add Quick Action',
      'icon': 'fas fa-plus',
      'cssClass': 'card-white',
      })
    }}
  </div>
  <div class="col">
    {{ include('components/card-button.twig', {
      'text': 'Add Quick Action',
      'icon': 'fas fa-plus',
      'cssClass': 'card-white',
      })
    }}
  </div>
</div>

{# cards #}
<div class="row">
  <div class="col-4">
    <div class="row-4">
      {{ include('components/card.twig', {
        'title': 'Most active contacts',
        'settings': true,
        'extracss': 'padding: 0;',
        'content': include('components/table.twig', {
          'columns': [
            {'title':'Given Name','type': 'varchar'},
            {'title':'Last Name','type': 'varchar'},
            {'title':'Age','type': 'varchar'},
          ]
          })
        }) 
      }}
    </div>
  </div>
  <div class="col-8">
    <div class="row-8">
      {{ include('components/card.twig', {
        'title': 'Most active contacts',
        'settings': true,
        'extracss': 'padding: 0;',
        'content': include('components/table.twig', {
          'columns': [
            {'title':'Given Name','type': 'varchar'},
            {'title':'Last Name','type': 'varchar'},
            {'title':'Age','type': 'varchar'},
          ]
          })
        }) 
      }}
    </div>
  </div>
</div>
<div class="row">
  {# Graph #}
  <div class="col-8">
    <div class="row-8">
      {{ include('components/card.twig', {
        'title': 'Tržby za rok 2023',
        'settings': true,
        'content': include('components/graph.twig', {
          'image': 'pie-chart-example-1.png'
          })
        }) 
      }}
    </div>
  </div>
  {# TODO list #}
  <div class="col-4">
    <div class="row-4">
      {{ include('components/card.twig', {
        'title': 'To do list',
        'settings': true,
        'content': include('components/todolist.twig', {
          'columns': [
            '',
            'Task Name',
            'Tag',
            ''
          ]
          })
        }) 
      }}
    </div>
  </div>

</div>
</div>