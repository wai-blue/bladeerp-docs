{# breadcrumbs#}
<div class="row">
    {{ include('components/breadcrumbs.twig', {
      'last':'',
      'this':'Dashboard'
    }) }}
</div>

{# top buttons #}
<div class="row">
  {% for i in 1..8 %}
    {{ include('components/quick-actions-buttons.twig') }}
  {% endfor %}
</div>

{# cards #}
  <div class="row">
    <div class="col-4">
      <div class="row-4">
        {{ include('components/card.twig', {
        'title': 'Most active contacts',
        'extracss': 'padding: 0;',
        'content': include('components/table.twig', {
          'columns': [
            {'title':'Given Name','type': 'varchar'},
            {'title':'Last Name','type': 'varchar'},
            {'title':'Age','type': 'varchar'},
          ]
        })
      }) }}
      </div>
    </div>
    <div class="col-8">
      <div class="row-8">
        {{ include('components/card.twig', {
        'title': 'Most active contacts',
        'extracss': 'padding: 0;',
        'content': include('components/table.twig', {
          'columns': [
            {'title':'Given Name','type': 'varchar'},
            {'title':'Last Name','type': 'varchar'},
            {'title':'Age','type': 'varchar'},
          ]
        })
      }) }}
      </div>
    </div>
  </div>
  <div class="row">
  {# Graph #}
    <div class="col-8">
      <div class="row-8">
        {{ include('components/card.twig', {
        'title': 'Tržby za rok 2023',
        'content': include('components/graph.twig', {
          'columns': [
            'Given Name',
            'Last Name',
            'Age'
          ]
        })
      }) }}
      </div>
    </div>
    <div class="col-4">
      {# TODO list #}
      <div class="row-4">
        {{ include('components/card.twig', {
        'title': 'To do list',
        'extracss': 'padding: 0;',
        'content': include('components/todolist.twig', {
          'columns': [
            '',
            'Task Name',
            'Tag',
            ''
          ]
        })
      }) }}
      </div>
    </div>
    
  </div>
</div>